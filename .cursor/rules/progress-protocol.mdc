---
description: PROGRESS 协议 — 会话开始读进度，结束更新 PROGRESS.md
alwaysApply: true
---

# PROGRESS 协议

完整说明：`/root/projects/_hq/PROGRESS_PROTOCOL.md`
全局路由：`/root/projects/_hq/ROUTING.md`（项目 → driver，只动自己负责的项目）

## 会话开始

1. 读取项目根目录 `PROGRESS.md`（若不存在则提示创建）
2. 从「下一步」继续，不要重复询问整体进度
3. 全局看板：`/root/projects/_hq/index.html`（先 `bash /root/projects/_hq/serve.sh`）

## 提交节奏（防丢工作）

- 每完成一个可独立成段的子任务 → `git commit`（message 说明做了什么）
- 最迟每 30 分钟提交一次；项目有远端就 `git push`
- 用户说「收工 / 走了 / 下班」→ 先提交并推送所有改动，再更新 PROGRESS
- 代码不完整也要提交，message 注明 `WIP`；系统有 autosave 安全网兜底（每 15 分钟）

## 跟上进度 / 交接检查（用户说「跟上进度」「检查进度」「Review」时）

1. 读项目 `PROGRESS.md`，总结上次进度（最近完成 / 下一步 / 备注）
2. `git log --oneline -10` 看最近提交；`git status` / `git diff` 看未提交改动
3. 汇报：上次做到哪、最近改了啥、工作区是否干净；**默认不改代码**，除非用户明确要求

## 会话结束

当用户说「更新 PROGRESS」「收工」「同步看板」或本段任务告一段落时：

1. 更新 `PROGRESS.md` 的 YAML frontmatter：`status`、`lastTool`（填 `cursor`）、`lastSession`（ISO 时间）、`environment`、`branch`、`progress`
2. 更新「下一步」「最近完成」
3. 若本项目成为唯一专注，将其他项目的 `focus` 改为 `in_progress` 或 `paused`
4. 运行：`python3 /root/projects/_hq/sync-progress.py`
5. 记忆入库：`git -C /root/projects/_hq add -A && git -C /root/projects/_hq commit -m "cursor 会话收尾" && git -C /root/projects/_hq push`

## 图片

- 看图用视觉模型（DSH 是 `mcp__vision__describe_image`）；用户贴不了图时，截图在 `/root/projects/pics/`

## 状态规则

- 全局只允许 1 个 `focus`
- 建议 WIP：`in_progress` ≤ 2
